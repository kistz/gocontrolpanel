<quad
  pos="{{pos.x}} {{pos.y}}"
  z-index="{{zIndex}}"
  scale="{{scale}}"
  rot="{{rot}}"
  hidden="{{boolToNum hidden}}"
  id="{{id}}"
  class="{{class}}"
  size="{{size.width}} {{size.height}}"
  halign="{{hAlign}}"
  valign="{{vAlign}}"
  opacity="{{boolToNum opacity}}"
  scriptevents="{{boolToNum scriptEvents}}"
  action="{{action}}"
  url="{{url}}"
  manialink="{{manialink}}"
  scriptaction="{{scriptAction}}"
  image="{{image}}"
  imagefocus="{{imageFocus}}"
  alphamask="{{alphaMask}}"
  bgcolor="{{bgColor}}"
  bgcolorfocus="{{bgColorFocus}}"
  bluramount="{{blurAmount}}"
  blendmode="{{blend}}"
  style="{{style}}"
  substyle="{{subStyle}}"
  styleselected="{{boolToNum styleSelected}}"
  colorize="{{colorize}}"
  modulatecolor="{{modulateColor}}"
  autoscale="{{boolToNum autoScale}}"
  keepratio="{{keepRatio}}"
  autoscalefixedwidth="{{boolToNum autoScaleFixedWidth}}"
  pincorners="{{pinCorners}}"
/>