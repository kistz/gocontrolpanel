<quad
  pos="{{ pos.x }} {{ pos.y }}"
  z-index="{{ zIndex }}"
  scale="{{ scale }}"
  rot="{{ rot }}"
  hidden="{{ hidden }}"
  id="{{ id }}"
  class="{{ class }}"
  size="{{ size.width }} {{ size.height }}"
  halign="{{ hAlign }}"
  valign="{{ vAlign }}"
  opacity="{{ opacity }}"
  scriptevents="{{ scriptEvents }}"
  action="{{ action }}"
  url="{{ url }}"
  manialink="{{ manialink }}"
  scriptaction="{{ scriptAction }}"
  image="{{ image }}"
  imagefocus="{{ imageFocus }}"
  alphamask="{{ alphaMask }}"
  bgcolor="{{ bgColor }}"
  bgcolorfocus="{{ bgColorFocus }}"
  bluramount="{{ blurAmount }}"
  blendmode="{{ blend }}"
  style="{{ style }}"
  substyle="{{ subStyle }}"
  styleselected="{{ styleSelected }}"
  colorize="{{ colorize }}"
  modulatecolor="{{ modulateColor }}"
  autoscale="{{ autoScale }}"
  keepratio="{{ keepRatio }}"
  autoscalefixedwidth="{{ autoScaleFixedWidth }}"
  pincorners="{{ pinCorners }}"
/>